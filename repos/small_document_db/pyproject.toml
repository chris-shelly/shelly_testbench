[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "smalldoc-db"
version = "0.1.0"
description = "Lightweight, pure-Python document-oriented database"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=7.0"]
